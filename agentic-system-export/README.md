# Orchestrator Agentic System

Utilities and CLI tooling for the Orchestrator workspace.

## Installation

```bash
python -m pip install -e .
```

## Console Commands

- `orchestrator` (safe default: production extractor)
- `orchestrator-extract`
- `orchestrator-context`
- `orchestrator-state`